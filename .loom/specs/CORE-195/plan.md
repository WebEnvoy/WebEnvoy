# Plan

## Steps

1. Add Core runtime self-check coverage for Xiaohongshu search, Xiaohongshu note detail, BOSS job search, and BOSS job detail using Lode package refs and Harbor refs-only identity/runtime/evidence facts.
2. Assert Run Record attribution for capability ref/version/source ref/lock ref/package ref/resource requirement ref/runtime session binding/evidence refs.
3. Assert result completion for public projections while keeping Core payload storage refs-only.
4. Assert cancellation, timeout, and user takeover/manual recovery terminal states with structured failure codes.
5. Add schema fixtures and conformance checks for real-site read-only success and user takeover records.
6. Validate Core runtime, schemas, conformance, typecheck, diff check, Loom fact-chain/verify, and Loom suite/carrier/evidence gates.
7. Commit, push, create PR, read back head/body metadata, and run PR metadata preflight.
