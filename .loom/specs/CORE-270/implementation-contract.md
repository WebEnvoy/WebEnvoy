# CORE-270 Implementation Contract

- Allowed writes: Core runtime task chain, a focused durable detail-target module/store if needed, API public-input validation, focused Core/API tests, and CORE-270 carriers.
- Preserve: Run Record ownership, refs-only output, existing search and BOSS fail-closed policy, Harbor/Lode owner boundaries.
- Prohibit: raw URL/note/xsec input, sensitive/browser material, cross-repository edits, BOSS production restore, external pages or writes.
- Validation: targeted Core/API typechecks/tests, full typecheck/test/lint, diff check, fact-chain and full-suite checks.
