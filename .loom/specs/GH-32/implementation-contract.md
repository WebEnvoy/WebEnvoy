# Implementation Contract

- Write scope: `AGENTS.md` and `.loom/**/GH-32*` carrier files.
- Forbidden scope: product docs, roadmap content, schema/API/runtime code, generated artifacts, and historical `INIT-0001` migration.
- Validation floor: whitespace check, Loom fact-chain checks, and hosted required checks.
