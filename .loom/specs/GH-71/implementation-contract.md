# Implementation Contract

- Write scope: `docs/README.md`, `docs/contracts/README.md`, `docs/draft/**`, `.loom/work-items/GH-71.md`, `.loom/progress/GH-71.md`, `.loom/specs/GH-71/*`, `.loom/status/current.md`, and necessary PR metadata only.
- Forbidden scope: product code, API/CLI/MCP/SDK/App/runtime implementation, final schemas, fixture files, generated artifacts, migrations, external source directories, other repositories, `docs/guides/`, issue closeout, merge, and historical `INIT-0001` carrier reuse.
- Validation floor: `git diff --check`, `.loom/**/*.json` validation, available Loom local checks, and hosted checks after PR creation.
