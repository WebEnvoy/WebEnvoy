{
  "name": "@webenvoy/api-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node dist/self-check.js",
    "lint": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js"
  }
}

