{
  "name": "@webenvoy/conformance",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @webenvoy/api-server build && tsc -p tsconfig.json",
    "typecheck": "pnpm --filter @webenvoy/api-server build && tsc -p tsconfig.json --noEmit",
    "test": "pnpm build && node dist/self-check.js",
    "smoke": "pnpm build && node dist/api-cli-smoke.js",
    "lint": "pnpm --filter @webenvoy/api-server build && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@webenvoy/api-server": "workspace:*",
    "@webenvoy/core-runtime": "workspace:*"
  }
}
