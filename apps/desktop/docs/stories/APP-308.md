# WebEnvoy App Human Workbench Story

## User Story

- Schema marker: `loom-user-story/v1`
- Story statement: 作为使用 WebEnvoy 管理网站工作的市场、运营或产品人员，我希望在一个面向人的 App 中管理账号与浏览器、使用站点技能、创建或观察所有入口产生的任务并直接消费业务结果，从而无需理解内部运行系统也能完成工作、处理异常并决定下一步。
- Actor: 使用 WebEnvoy 管理网站账号、浏览器环境和网页任务的市场、运营、产品等人类业务用户。
- Capability: 通过 App 的 Work、Browser、Library 三个业务域创建和观察任务、管理账号与浏览器实例、使用站点技能，并在异常或执行确认边界出现时继续、接管或终止工作。
- Outcome: 用户无需理解 Core、Harbor、Lode、refs 或 runtime 状态，就能通过适合业务输出类型的标准视图或技能提供的可选视图，看清可执行动作、业务结果、失败原因和下一步；确有排查需要且存在实际诊断记录时，再通过独立入口查看技术日志。
- Business value: App 成为 WebEnvoy 面向人类的统一工作台，让 App、CLI、MCP、API、SDK 和 Agent 产生的任务共享可理解、可恢复、可消费的桌面体验。
- Out of scope:
  - 不把 App 变成 Agent 聊天容器、任意网页自动化入口或 owner truth 存储。
  - 不把小红书当成产品边界；它只承担当前 live validation slice。
  - BOSS 生产任务保持 deferred，不展示为可运行。
  - 不在本 story 决定生产组件、像素、最终路由、Core/Harbor/Lode 接口实现或真实写入能力。
  - 不把运行 refs、内部 evidence 或日志作为默认业务内容。
  - 不在本 story 实现技能输出组件的生产沙箱、资源协议、签名分发或跨版本迁移机制。

## Product Context

- Vision / roadmap locator: `VISION.md`, `DESIGN.md`, `docs/adr/0009-human-workbench-information-architecture.md`
- Host issue / notes locator: `https://github.com/WebEnvoy/App/issues/298`, `https://github.com/WebEnvoy/App/issues/308`
- Discussion summary locator: 当前 Codex 任务中关于多入口、人类 App、Work/Browser/Library、业务结果优先、统一执行策略、provider 修复、任务线程双分组和人工审核 Gate 的讨论与用户修订输入。

## Acceptance Scenarios

所有验收场景都使用业务可读的 Given / When / Then，描述可观察的产品行为，
不描述实现步骤或测试脚本。

<a id="scenario-s1"></a>
### Scenario S1: 从账号身份开始工作

- Scenario id: `S1`
- Scenario locator: `docs/stories/APP-308.md#scenario-s1`
- Dimension: `happy_path`

**Given**

用户拥有一个可用或需要登录的账号身份。

**When**

用户在 Browser 中选择该账号身份。

**Then**

- App 展示该账号当前是否可用以及“打开浏览器”或“选择技能”的主要动作。
- 需要登录时，App 直接提供登录入口和完成后的继续路径。
- 手动浏览不会被表述为自动任务或业务结果。

<a id="scenario-s2"></a>
### Scenario S2: 从站点技能开始工作

- Scenario id: `S2`
- Scenario locator: `docs/stories/APP-308.md#scenario-s2`
- Dimension: `alternative_path`

**Given**

用户在 Library 中找到一个满足业务目标的站点技能。

**When**

用户查看技能用途、所需输入和预期结果，并点击“去使用”。

**Then**

- App 打开已预选该站点技能的任务创建页，并只列出兼容账号身份。
- 任务输入表单与该技能声明的输入字段、类型、选项和约束一致。
- 任务创建前，用户可以看清将读取、准备、提交或改变什么。
- 技能包、内部测试和维护信息不会抢占主要流程。

<a id="scenario-s3"></a>
### Scenario S3: 从 Work 创建任务

- Scenario id: `S3`
- Scenario locator: `docs/stories/APP-308.md#scenario-s3`
- Dimension: `happy_path`

**Given**

用户已经进入 Work，并知道希望完成的业务目标。

**When**

用户从全局“任务”入口进入空状态创建页，选择推荐的“账号身份 + 站点技能”组合，或自行选择兼容的账号身份和站点技能。

**Then**

- 空状态创建页不预设任务线程；推荐项以“账号身份 + 站点技能”组合呈现，选择后再建立任务上下文。
- App 根据所选站点技能展示对应输入表单，而不是开放式通用输入框。
- App 在提交前校验必填项、字段类型、选项和约束，并展示任务目标、当前执行方式及其配置来源。
- 创建页 Composer 展开后展示当前技能声明的全部业务动作类型，每类可独立选择“自动、确认、禁止”；选择随提交写入目标线程，不修改“我的技能默认”或全局默认。
- 创建后，任务进入统一的运行、结果和异常恢复体验。
- 若缺少技能、兼容账号或可用浏览器，App 展示可执行的补救路径而不是创建不可运行任务。

<a id="scenario-s4"></a>
### Scenario S4: 观察其他入口创建的任务

- Scenario id: `S4`
- Scenario locator: `docs/stories/APP-308.md#scenario-s4`
- Dimension: `alternative_path`

**Given**

CLI、MCP、API、SDK 或 Agent 已经创建一个 WebEnvoy 任务。

**When**

用户打开 Work 并查看该任务。

**Then**

- 该任务与 App 创建的任务使用一致的业务状态、结果和恢复体验。
- 非 App 创建渠道在已结束回合的时间脚标右侧标识；App 渠道默认隐藏，且不会按技术入口拆成不同任务产品。

<a id="scenario-s5"></a>
### Scenario S5: 查看任务结果摘要

- Scenario id: `S5`
- Scenario locator: `docs/stories/APP-308.md#scenario-s5`
- Dimension: `happy_path`

**Given**

一个任务已经结束，或已经产生可以消费的阶段性业务结果。

**When**

用户打开该任务。

**Then**

- App 首先展示任务目标、完成状态、结果摘要、结果数量或写操作状态以及可执行下一步。
- 部分完成、结果未知和仍在处理中使用不同业务状态，不会统一显示为成功。
- 原页面入口按业务需要位于次级层级；只有存在实际诊断记录时，才提供独立的技术日志入口。
- 未知结果类型仍有可读的通用详情，而不是把原始数据作为首选结果。

<a id="scenario-s6"></a>
### Scenario S6: 空结果或任务失败

- Scenario id: `S6`
- Scenario locator: `docs/stories/APP-308.md#scenario-s6`
- Dimension: `negative_path`

**Given**

任务返回空结果、输入错误、技能失效、登录失效、访问限制或未知结果。

**When**

用户查看该任务。

**Then**

- App 先说明业务影响和可执行下一步，例如修改输入、重新登录、重试、打开现场或终止。
- 失败回合直接展示业务影响和恢复动作；只有存在实际诊断记录且用户主动查看时，才展示技术日志。
- 不存在真实结果时，App 不会用样例或历史结果伪装成功。

<a id="scenario-s7"></a>
### Scenario S7: Provider 不可用并恢复

- Scenario id: `S7`
- Scenario locator: `docs/stories/APP-308.md#scenario-s7`
- Dimension: `environment_interruption`

**Given**

账号或任务所需的浏览器 provider 未安装、损坏、版本不兼容或无法启动。

**When**

用户从 Browser 或原任务选择修复问题。

**Then**

- App 根据 provider 来源提供检测、下载、安装、更新、修复、重检或打开外部管理入口。
- Harbor 管理的 Provider 可以在 App 内完成检测、下载、安装、更新、修复和启动验证；系统已安装的 Provider 只提供定位、版本检查和重检；外部管理的 Provider 提供打开其管理器和返回 App 后重检，不把后两类表述为 App 可一键修复。
- 安装或更新前，App 展示受影响的账号身份；完成后必须通过版本、完整性和启动验证才恢复为可用。
- 恢复成功后返回原账号身份或任务上下文；失败时保留清楚、可执行的恢复选择。

<a id="scenario-s8"></a>
### Scenario S8: 需要人工介入

- Scenario id: `S8`
- Scenario locator: `docs/stories/APP-308.md#scenario-s8`
- Dimension: `environment_interruption`

**Given**

任务因登录、验证码、访问限制、文件选择或未知页面状态需要人工操作。

**When**

App 通知用户，用户选择打开对应浏览器现场，完成操作后点击“已完成，继续”或反馈“无法完成”。

**Then**

- App 在收到“已完成，继续”后校验页面、登录或资源状态是否满足恢复条件。
- 校验成功时，系统结束人工接管并继续完成原任务。
- 校验失败时，App 说明仍缺少什么，并允许再次接管、重试校验或终止；不会直接把人工介入报为任务成功。

<a id="scenario-s9"></a>
### Scenario S9: 当前动作需要单次确认

- Scenario id: `S9`
- Scenario locator: `docs/stories/APP-308.md#scenario-s9`
- Dimension: `security_permission`

**Given**

任务中的业务动作属于技能已声明的动作，或浏览器环境操作属于系统已定义且可分类的动作；其有效执行方式按适用的当前动作、当前线程、“我的技能默认”和全局默认计算为“确认”。

**When**

App 请求用户决定是否允许当前具体动作。

**Then**

- App 清楚展示账号身份、业务动作、目标和待提交内容摘要，并提供“允许这一次”或“拒绝这一次”。
- 单次决定只覆盖当前动作，在动作完成、取消、超时或目标变化后失效，不静默修改当前线程、“我的技能默认”或全局默认。
- 单次确认不提供记住决定或修改后续配置的选项；用户只能从线程 Composer 主动修改当前线程后续回合的执行方式。有效方式为“禁止”时停止执行并引导用户主动修改配置，不提供临时绕过。
- 超出技能声明动作、目标或有效期的操作直接停止，不提供扩大技能声明的入口。
- 无关联技能或线程的浏览器环境操作不要求技能声明，但必须是系统已定义且目标匹配的业务动作；未定义、目标不匹配或无法分类时停止执行。
- 所有任务入口使用同一执行策略结果，不为单个站点建立独立审批流程。

<a id="scenario-s10"></a>
### Scenario S10: 管理浏览器实例生命周期

- Scenario id: `S10`
- Scenario locator: `docs/stories/APP-308.md#scenario-s10`
- Dimension: `edge_case`

**Given**

用户已经选择一个账号身份，且该身份拥有空闲、运行中或需要接管的浏览器实例。

**When**

用户查看该账号的浏览器实例。

**Then**

- App 根据当前状态提供启动、查看、接管、释放或停止等有效动作。
- 用户可以看清当前使用者、站点和是否需要处理，但内部实例标识默认隐藏。
- 同一账号身份已有活动实例时，“打开”复用该实例并提供查看或接管，不静默启动第二个争用同一环境的实例。
- 停止或释放后，账号状态及时更新，不继续显示为正在使用。

<a id="scenario-s11"></a>
### Scenario S11: 按需查看来源与诊断

- Scenario id: `S11`
- Scenario locator: `docs/stories/APP-308.md#scenario-s11`
- Dimension: `edge_case`

**Given**

业务用户需要核对结果来源或排查失败。

**When**

用户主动打开原页面来源，或在确有技术记录时进入独立的技术日志入口。

**Then**

- 原页面来源作为业务结果的次级入口，不与任务回合的主要内容争夺层级。
- 业务执行步骤已经位于对应回合的“正在执行”或“已处理”折叠区，不再通过额外详情区重复展示。
- 只有存在真实诊断记录时，独立技术日志入口才展示可供排查的记录；关闭后，业务结果、失败原因和下一步保持不变。

<a id="scenario-s12"></a>
### Scenario S12: 创建账号身份

- Scenario id: `S12`
- Scenario locator: `docs/stories/APP-308.md#scenario-s12`
- Dimension: `happy_path`

**Given**

用户没有适合目标站点或任务的账号身份。

**When**

用户在 Browser 或任务创建页选择“创建账号身份”，选择是否需要账号登录，并选择浏览器 Provider 与环境配置。

**Then**

- App 创建独立、持久的身份环境，并将它加入 Browser 和兼容任务的账号选择列表。
- 需要登录时，用户输入完整目标网站 URL；App 创建环境后从该网址打开浏览器，完成登录校验后同步账号头像、昵称、平台 ID 和登录状态。
- 无需登录时，用户只需命名身份，不填写站点账号或账号标识；它仍是绑定当前站点的匿名身份，只用于该站点兼容的免登录技能，并在身份集合中按该站点展示和排序。
- 创建表单只展示所选 Provider 支持的环境配置；代理、地区、语言、时区和指纹设置可以使用推荐值、一键随机兼容参数或进入高级设置。
- 密码、Cookie、令牌和浏览器档案内容不会作为普通表单数据展示；用户主动创建完整副本时，由 Provider 在本机克隆 Profile，App 不读取或保存原始凭据。
- 创建完成后，App 清楚展示该身份是否可用、需要登录或需要修复。

<a id="scenario-s13"></a>
### Scenario S13: 管理站点技能生命周期

- Scenario id: `S13`
- Scenario locator: `docs/stories/APP-308.md#scenario-s13`
- Dimension: `negative_path`

**Given**

一个站点技能存在可用、需要更新、可能失效或需要修复的状态。

**When**

用户在 Library 中查看或管理该技能。

**Then**

- App 用业务语言说明技能用途、当前可用性、版本变化和受影响任务。
- 更新改变技能声明的业务动作、目标或动作分类时，App 在安装前展示变化摘要；新增或重新分类的动作不静默继承旧执行方式，用户需要采用或修订新版本的推荐配置。
- 已移除的动作不再出现在新任务配置中；历史回合仍保留执行时的动作分类、有效执行方式和来源。
- 用户获得更新、停用、重新启用或选择替代技能等有效动作；停用后不能再创建任务，已有结果仍可查看。
- 未通过验证的修复或样例不会被展示为已恢复的生产能力。

<a id="scenario-s14"></a>
### Scenario S14: 配置动作分级与执行方式

- Scenario id: `S14`
- Scenario locator: `docs/stories/APP-308.md#scenario-s14`
- Dimension: `security_permission`

**Given**

用户希望为不同入口产生的任务和浏览器操作配置一致、可追溯且可按当前工作修订的执行方式。

**When**

用户分别在 Settings、站点技能安装或详情、线程 Composer 和单次动作确认中管理执行方式。

**Then**

- 全局用户配置按“读取和下载、填写但不提交、发布或提交、危险行为”四类业务动作设置“自动、确认、禁止”的默认执行方式。
- “读取和下载”只覆盖读取内容、打开非可执行文件查看以及创建新的下载文件；覆盖、移动或删除已有文件，以及打开或执行可执行文件，必须归为“危险行为”，无法确认目标或分类时停止执行。
- 站点技能声明稳定的业务动作、动作分类、目标范围和提交边界，并提供推荐执行配置；安装时用户可以直接使用推荐配置或修订后安装，安装结果作为“我的技能默认”。
- 官方推荐技能的每个发布版本必须经过声明审核，确认业务动作、目标范围和提交边界完整且与实际执行一致；审核不替用户决定执行方式。
- 线程 Composer 的摘要展示当前有效执行方式及来源；展开后展示该技能声明的全部业务动作类型，每类可独立修订当前线程，也可以显式选择“保存为该技能的默认设置”，不静默覆盖“我的技能默认”。
- 当前动作按“当前动作单次决定 > 当前线程修订 > 我的技能默认 > 全局用户配置”的顺序采用最具体配置；技能包推荐配置只用于安装初始化，不参与安装后的实时覆盖计算。
- 单次决定只针对当前具体动作；浏览器环境操作没有关联技能或线程时，只消费适用的全局配置和当前动作决定。
- 用户可以选择危险动作自动执行；App 使用常驻醒目图标提示风险，说明文字可以关闭，任务结果与记录保留当时的有效执行方式及来源。
- 系统不替用户选择更严格的方式，但必须核对技能声明与实际业务动作一致；未声明、目标不匹配或无法分类的任务动作停止执行。无关联技能或线程的浏览器环境操作必须匹配系统已定义的动作和目标，否则同样停止执行。

<a id="scenario-s15"></a>
### Scenario S15: 按站点和业务场景浏览技能

- Scenario id: `S15`
- Scenario locator: `docs/stories/APP-308.md#scenario-s15`
- Dimension: `happy_path`

**Given**

Library 中存在来自多个站点、服务不同业务场景的站点技能。

**When**

用户打开 Library，选择站点分组或业务场景标签进行筛选。

**Then**

- Library 首先按站点组织技能，并在每个技能上显示所属站点。
- 技能使用数据采集、内容发布、内容下载、内容浏览等业务场景标签。
- 用户可以按一个或多个标签筛选，并与站点分组或搜索条件共同使用。
- 没有匹配技能时，App 展示当前筛选条件和清除筛选入口，而不是空白页面。

<a id="scenario-s16"></a>
### Scenario S16: 按站点技能要求填写任务

- Scenario id: `S16`
- Scenario locator: `docs/stories/APP-308.md#scenario-s16`
- Dimension: `negative_path`

**Given**

用户选择的站点技能声明了必填项、可选项、字段类型、候选值、默认值和校验约束。

**When**

App 展示任务创建表单，用户填写并尝试创建任务。

**Then**

- 线程内 Composer 的站点技能和账号身份已经固定，只读展示或省略；全局创建入口才允许选择兼容的账号身份和站点技能。
- Composer 只呈现该技能声明的结构化业务输入以及附件、校验状态和提交操作，不提供模型、语言、权限模式或自由对话控制。
- 字段标签、说明、控件、默认值和候选项与技能声明一致；长文本仍是明确的多行业务字段，不是通用聊天框。
- 输入不满足必填、类型或约束时，App 在对应字段说明问题并阻止创建。
- 输入有效时，提交的结构化输入按时间顺序成为线程内的新任务回合；回合顶部的只读业务输入卡与 Composer 共用同一字段定义。
- 同一线程一次只允许一个活动回合；回合运行中 Composer 仍保留并允许编辑下一次业务输入和附件，但提交操作禁用，并说明需等待当前回合结束。
- 未提交内容按线程保留：切换线程或重启 App 后恢复字段值和附件清单；附件在提交前不上传或复制，恢复后重新校验本机文件是否仍可读取，失效或读取失败时标明对应附件并阻止提交。
- 用户可以显式移除附件或清空草稿；校验失败保留当前输入。只有新回合被接受后，Composer 才清空已提交的业务输入和附件，并继续保留线程上下文。

<a id="scenario-s17"></a>
### Scenario S17: 消费数据采集结果

- Scenario id: `S17`
- Scenario locator: `docs/stories/APP-308.md#scenario-s17`
- Dimension: `happy_path`

**Given**

数据采集任务返回一组结构化记录，或只完成了部分记录。

**When**

用户打开任务结果。

**Then**

- App 使用字段明确的表格或列表展示记录，而不是把结果压缩成状态文字。
- 用户可以看到返回数量、字段含义、空值、部分完成和失败记录数量。
- 每条记录在需要时可以打开业务详情或原始页面，来源信息不抢占结果主体。
- 批量结果在列表下方显示“共 X 条数据”，尚有未展示记录时提供“再展示 Y 条”；单结果任务不展示批量分页控件。
- 批处理先显示已选数量、其中可处理数量和动作影响，只作用于用户明确选择且适用的记录；批量导出、重试或其他技能声明的批量动作逐项返回成功、失败或跳过结果，部分成功不会被汇总为全部成功。

<a id="scenario-s18"></a>
### Scenario S18: 消费内容浏览结果

- Scenario id: `S18`
- Scenario locator: `docs/stories/APP-308.md#scenario-s18`
- Dimension: `happy_path`

**Given**

内容浏览或读取任务返回文章、帖子、评论、作者资料或媒体内容。

**When**

用户打开任务结果。

**Then**

- App 以可阅读形式展示标题、作者、时间、正文、媒体和相关业务字段。
- 列表结果可以进入单条内容详情，并保留返回列表的路径。
- 内容缺失、被删除或只有部分字段时，App 明确标识缺失部分，不用空白区域代替说明。
- 图片、音频、视频或其他媒体无法内嵌预览时，App 展示文件类型、大小和可用状态，并提供安全的系统打开、下载或针对失败项的恢复入口。

<a id="scenario-s19"></a>
### Scenario S19: 消费内容下载结果

- Scenario id: `S19`
- Scenario locator: `docs/stories/APP-308.md#scenario-s19`
- Dimension: `edge_case`

**Given**

内容下载任务包含一个或多个文件，并可能出现部分下载失败。

**When**

用户打开任务结果。

**Then**

- App 按文件展示名称、类型、大小、保存位置和成功、失败或跳过状态。
- 已下载文件提供打开文件或所在位置的入口。
- 失败文件说明原因并提供针对失败项的重试入口，不要求重新下载全部内容。
- 下载默认创建新文件；目标已存在时不静默覆盖。覆盖、移动、删除或执行文件需要按 Scenario S14 的“危险行为”重新判断执行方式，无法确认目标时停止。

<a id="scenario-s20"></a>
### Scenario S20: 消费内容发布或写操作结果

- Scenario id: `S20`
- Scenario locator: `docs/stories/APP-308.md#scenario-s20`
- Dimension: `security_permission`

**Given**

任务准备或执行了内容发布、修改、发送或其他写操作。

**When**

用户打开任务结果。

**Then**

- App 展示目标站点、账号、准备或提交的内容摘要以及操作时间。
- App 明确区分未提交、已提交、失败和结果未知，写前预览不会被显示为已提交。
- 已提交结果展示站点返回的业务状态和可用页面入口；失败或结果未知时展示下一步处理方式。

<a id="scenario-s21"></a>
### Scenario S21: 查找和组织账号身份

- Scenario id: `S21`
- Scenario locator: `docs/stories/APP-308.md#scenario-s21`
- Dimension: `happy_path`

**Given**

用户拥有多个站点、分组和状态不同的账号身份。

**When**

用户打开 Browser，搜索或筛选账号身份。

**Then**

- App 支持按站点、分组、标签、登录状态、provider 和最近使用时间查找账号身份。
- 账号身份集合支持按“最近使用”或“站点名称”排序，排序只改变列表顺序。
- 每个列表项优先展示账号名称、站点、是否可用和主要动作；代理、指纹和路径等细节进入详情。
- 当前筛选、结果数量和清除筛选入口始终可见。

<a id="scenario-s22"></a>
### Scenario S22: 理解身份、环境和实例关系

- Scenario id: `S22`
- Scenario locator: `docs/stories/APP-308.md#scenario-s22`
- Dimension: `edge_case`

**Given**

用户查看一个账号身份及其浏览器运行状态。

**When**

用户打开账号身份详情或尝试再次启动它。

**Then**

- App 将账号身份作为主要对象；一个账号身份对应一个持久浏览器环境，运行实例是该环境的一次临时运行。
- 同一环境同一时间最多拥有一个活动可控实例；再次打开时复用现有实例并提供查看、接管或停止。
- 需要独立环境时，用户创建或复制为新的账号身份，而不是让两个实例同时修改同一环境。

<a id="scenario-s23"></a>
### Scenario S23: 导入已有账号环境

- Scenario id: `S23`
- Scenario locator: `docs/stories/APP-308.md#scenario-s23`
- Dimension: `alternative_path`

**Given**

用户已有受支持的 WebEnvoy 环境、系统浏览器档案或 provider 管理的环境。

**When**

用户在 Browser 中选择“导入账号身份”并选择来源。

**Then**

- App 在导入前展示来源、站点、provider、兼容性和将被纳入管理的内容摘要。
- 来源正在使用、版本不兼容、不可访问或与现有身份冲突时，App 阻止导入并给出对应处理方式。
- 导入成功后创建独立账号身份并验证可启动性；敏感材料只由本机环境管理，不展示原始 Cookie 或凭据。

<a id="scenario-s24"></a>
### Scenario S24: 配置并验证代理和网络

- Scenario id: `S24`
- Scenario locator: `docs/stories/APP-308.md#scenario-s24`
- Dimension: `negative_path`

**Given**

账号身份使用代理，或目标站点对网络出口有明确要求。

**When**

用户创建、编辑或启动账号身份。

**Then**

- App 允许选择无代理或配置受支持的代理，并在保存或启动前测试连通性、出口地区和目标站点可达性。
- 代理出口与地区、语言或时区配置明显冲突时，App 展示可修改的冲突项。
- 代理失败时，用户可以修改、重试或明确选择其他网络；App 不静默切换网络。

<a id="scenario-s25"></a>
### Scenario S25: 配置浏览器指纹

- Scenario id: `S25`
- Scenario locator: `docs/stories/APP-308.md#scenario-s25`
- Dimension: `alternative_path`

**Given**

所选 provider 支持一个或多个浏览器环境预设和指纹设置。

**When**

用户创建、复制或编辑账号身份。

**Then**

- App 提供推荐预设、复制已有环境设置或自定义受支持字段的入口。
- App 只展示当前 provider 实际支持的设置，并在保存前指出不兼容组合。
- 列表页只展示浏览器、系统和地区等摘要；完整设置在身份详情中按需展开。

<a id="scenario-s26"></a>
### Scenario S26: 同步登录状态

- Scenario id: `S26`
- Scenario locator: `docs/stories/APP-308.md#scenario-s26`
- Dimension: `environment_interruption`

**Given**

用户在账号身份浏览器中完成登录、退出登录，或登录状态已经过期。

**When**

用户反馈登录已完成、刷新身份状态、重启 App，或系统检测到站点登录变化。

**Then**

- App 针对具体站点重新校验，并展示已登录、需要登录、状态未知或需要人工验证及最近确认时间。
- 校验成功后，兼容任务入口立即使用更新状态；校验失败时保留打开现场和再次校验入口。
- 浏览器关闭或 App 重启不会把历史登录状态继续显示为当前已确认状态。

<a id="scenario-s27"></a>
### Scenario S27: 选择启动页面和恢复方式

- Scenario id: `S27`
- Scenario locator: `docs/stories/APP-308.md#scenario-s27`
- Dimension: `happy_path`

**Given**

账号身份可用于手动浏览，并可能存在默认站点、指定网址或上次未结束页面。

**When**

用户点击“打开浏览器”。

**Then**

- App 允许打开默认站点、输入指定网址，或在安全可恢复时恢复上次页面。
- 启动后，用户可以查看当前页面并进入人工操作现场。
- 用户直接关闭浏览器时，App 及时同步实例已关闭，而不是继续显示运行中。

<a id="scenario-s28"></a>
### Scenario S28: 恢复异常浏览器实例

- Scenario id: `S28`
- Scenario locator: `docs/stories/APP-308.md#scenario-s28`
- Dimension: `environment_interruption`

**Given**

浏览器启动失败、异常退出、残留进程占用环境，或环境被其他进程锁定。

**When**

用户打开账号身份或尝试再次启动。

**Then**

- App 区分可重新连接、可清理残留、需要关闭其他实例、需要修复环境和无法自动恢复等情况。
- 用户可以执行对应的重新连接、清理、重启或复制环境操作，并看到处理结果。
- 清理或修复失败时，App 保留原环境和诊断入口，不把失败状态改写成可用。

<a id="scenario-s29"></a>
### Scenario S29: 编辑、复制或删除账号身份

- Scenario id: `S29`
- Scenario locator: `docs/stories/APP-308.md#scenario-s29`
- Dimension: `security_permission`

**Given**

用户需要修改、复制或移除一个已有账号身份。

**When**

用户从身份详情选择对应操作。

**Then**

- 实例运行时，App 只允许安全的显示信息修改；影响环境的设置要求先停止实例。
- App 将“完整复制”和“仅复制环境配置”作为并列选择；完整复制保留账号资料、本机登录状态和环境配置，仅环境副本不包含账号资料与站点数据。
- 完整复制由 Provider 在同一设备本机完成；两种副本都生成新的身份 ID 和数据目录，并重置活动实例、进程锁、临时文件和运行时连接。
- 删除时，App 区分“从 App 移除”和“删除本机环境数据”，说明活动实例和历史任务引用的处理结果。
- 历史任务仍保留原账号名称和已删除标记，不因删除身份而失去业务记录。

<a id="scenario-s30"></a>
### Scenario S30: 在账号身份与任务之间往返

- Scenario id: `S30`
- Scenario locator: `docs/stories/APP-308.md#scenario-s30`
- Dimension: `alternative_path`

**Given**

用户正在查看账号身份或使用该身份执行的任务。

**When**

用户从身份选择技能创建任务，或从任务打开关联身份和浏览器现场。

**Then**

- 两个入口保持相同账号身份、站点和任务上下文，并提供返回原页面的路径。
- 任务创建时展示任务结束后的实例处理方式；默认释放任务控制，但不强制关闭用户正在使用的实例。
- 任务结束后，App 按所选策略保持或停止实例，并同步 Browser 与 Work 中的状态。

<a id="scenario-s31"></a>
### Scenario S31: 切换任务线程组织方式

- Scenario id: `S31`
- Scenario locator: `docs/stories/APP-308.md#scenario-s31`
- Dimension: `alternative_path`

**Given**

Work 中存在由不同站点技能和账号身份产生的任务线程，每个线程固定绑定一个站点技能和一个账号身份，并包含一个或多个任务回合。

**When**

用户打开任务线程的整理入口，并切换分组或排序方式。

**Then**

- “按技能”直接展示站点技能，再以头像和账号名称展示使用该技能的账号身份线程，不在站点技能外重复增加站点层级；技能标题只保留站点图标，不重复显示站点名称。“按身份”先展示账号身份，再展示该身份使用的站点技能线程。线程行不显示业务输入等副标题。
- Work、Browser 和 Library 的左栏下半区始终只展示同一任务线程列表；账号身份和站点技能分别在中栏集合页中浏览、搜索、排序和管理。
- 两种分组展示同一批任务线程；切换后当前线程和回合保持不变，不复制任务、结果或运行事实。
- 分组、排序和新建任务入口保持可发现且支持键盘操作；整理入口提供分组方式与排序方式，具体位置和显隐方式由设计规范定义。
- 按技能分组时，用户可以从对应技能上下文选择另一个兼容账号身份创建任务；没有兼容身份时进入创建身份恢复路径，具体触发控件由设计规范定义。
- 用户可以按优先级或最近更新排序；排序只改变前端列表顺序，不改变线程内容与运行事实。
- App、CLI、MCP、API、SDK 或 Agent 使用相同站点技能和账号身份提交的新业务输入，追加为匹配线程内的新任务回合。所有入口遵守同一线程单活动回合约束：已有活动回合时，外部输入必须明确进入可见队列或被拒绝并返回原因，不能启动第二个活动回合。
- App 记住用户选择的分组和排序方式，并在下次打开 Work 时恢复。

<a id="scenario-s32"></a>
### Scenario S32: 查看任务线程的完整时间线

- Scenario id: `S32`
- Scenario locator: `docs/stories/APP-308.md#scenario-s32`
- Dimension: `happy_path`

**Given**

一个任务线程包含由多次业务输入产生的多个任务回合，回合可能处于完成、运行中、等待人工处理、部分完成或未提交状态，并拥有各自的业务结果与文件。

**When**

用户打开任务线程、使用回合导航定位历史或当前回合，并从某个回合中打开业务结果或文件。

**Then**

- 中栏按时间顺序展示当前线程的全部回合；每个回合按“业务输入 -> 正在执行或已处理 -> 业务结果或未解决项 -> 回合脚标”的顺序组织，运行中或等待人工处理的回合不伪装成终态结果。
- 执行中显示“正在执行”；完成后显示“已处理”和执行时长。该区域默认收起，展开后按时间顺序显示打开网址、滚动页面、点击控件、填写输入和读取结果等原子化页面动作；原始工具参数、重复内部事件、日志和 trace 不进入默认回合内容。
- 完成、部分完成、未提交和失败回合明确说明业务结果与未解决项，不增加额外总结标题；需要人工介入时，展示接管、完成反馈、系统校验和校验成功后续跑的入口。
- 回合导航只滚动定位中栏中的对应回合，不切换、隐藏或替换中栏内容，也不改变右栏当前预览。
- 右栏默认折叠，或恢复该线程上一次的折叠或展开状态；它不与导航轨或最新回合自动关联，只有用户从中栏明确打开结果或文件时，才创建或激活对应预览标签。
- 已结束回合在左侧脚标中只显示时间；非 App 创建渠道在时间右侧同行展示，App 创建渠道默认隐藏。运行中或等待中的回合不显示结束时间。
- 定位或预览回合不会改变线程、复制运行事实或丢失其他回合结果；技术日志只通过 Scenario S11 的独立入口按需查看。
- 创建任务、填写 Composer、单次确认、人工接管、回合导航、结果预览和面板开关都可仅用键盘完成；焦点可见，页面或面板关闭后回到合理触发位置，字段错误与对应控件关联，状态不只依赖颜色表达。
- 在 200% 缩放及窄于 720px 的窗口中，主要业务输入和当前回合仍保持可用；右栏以全宽或覆盖方式打开，不把中栏压缩到不可操作，折叠面板前会把焦点移回可见区域。减少动态效果偏好下不使用平滑定位或非必要高亮动画。

<a id="scenario-s33"></a>
### Scenario S33: App 或网络中断后恢复任务

- Scenario id: `S33`
- Scenario locator: `docs/stories/APP-308.md#scenario-s33`
- Dimension: `environment_interruption`

**Given**

线程中有一个已经提交且可能仍在执行的任务回合，App 退出、重启、失去网络连接或暂时无法取得运行状态。

**When**

用户重新打开 App 或恢复网络并返回该线程。

**Then**

- App 先重新读取任务与运行事实，不因本地页面状态丢失而自动重新提交或重复执行已经接受的回合。
- 能确认运行状态时，App 恢复原回合的运行、等待、完成或失败状态，并保留提交内容、结果和当前线程上下文。
- 暂时无法确认状态时，回合显示“状态未知”，Composer 保留草稿但继续禁用提交，并提供重连、重新检查或终止等符合当前事实的动作，不把未知显示为失败或成功。
- 只有确认原回合已结束或未被接受后，用户才能再次提交；重试必须是显式的新决定，并避免与仍可能活动的原回合并行。

<a id="scenario-s34"></a>
### Scenario S34: 使用技能提供的可选结果视图

- Scenario id: `S34`
- Scenario locator: `docs/stories/APP-308.md#scenario-s34`
- Dimension: `security_permission`

**Given**

站点技能已经声明结构化输出，并可选提供一个与该输出版本兼容的结果视图。

**When**

用户从任务回合明确打开结果；该技能可能只使用 App 标准视图，也可能提供适合业务场景的自定义视图。

**Then**

- 结构化输出始终是可保存、查看和导出的结果事实；技能视图只负责展示，不替代结果本身。
- App 对集合、单条内容、媒体、文件和写操作优先使用一致的标准组件；技能视图只用于地图、图表、对比或其他标准组件不足以表达的结果。
- 中栏继续展示 App 原生的业务摘要或列表；技能视图只在用户明确打开后进入右栏，并同时保留结构化数据入口。
- App 在技能视图中标明提供该视图的站点技能及视图版本，让用户能区分 App 标准视图与技能提供的视图。
- 技能视图只接收当前结果所需的结构化数据，并通过受限动作入口发送业务意图；它不能直接访问 Cookie、账号环境、文件系统或 Runtime API。视图发起的每个动作都必须回到 App，以当前业务动作和目标重新计算执行方式，并沿用单次确认、禁止、失败和焦点恢复规则。
- 技能视图缺失、加载失败、越权或版本不兼容时，App 隔离该视图的失败并自动回退到标准组件或通用表格、键值详情；结构化结果、历史结果和其他预览不受影响，并用业务语言说明已切换到兼容视图。
- 每个任务回合保留生成结果时的视图兼容声明；技能升级不会把新视图静默套用到历史结果，历史结果仍可通过结构化输出消费。
- 站点技能发布版本包含视图资源兼容信息；“我的技能默认”只表示用户保存的执行方式，两者不使用同一种版本语义。

## Story Readiness

- Schema marker: `loom-story-readiness/v1`
- Decision: `confirmed`
- Rationale: actor、outcome、业务价值、范围和六类风险场景已经明确，且本轮修订后的场景内部一致；Story Business Confirmation 已于 2026-07-19 由用户明确确认。
- Story locator: `docs/stories/APP-308.md`
- Checks:
  - actor_specificity: pass
  - outcome_clarity: pass
  - value_signal: pass
  - acceptance_scenario_quality: pass
  - unresolved_blockers: none
  - story_size: pass for FR-level design intake; production implementation remains split by validated journeys
- Missing inputs: none for story readiness
- Fallback to: `null`

## Story Business Confirmation

- Schema marker: `loom-story-business-confirmation/v1`
- Decision: `confirmed`
- Business Confirmation locator: `docs/stories/APP-308.md#story-business-confirmation`
- Confirmed by: 用户
- Confirmation source: 2026-07-19 当前 Codex 任务中用户明确回复“批准当前用户故事和高保真原型，继续目标”；该确认覆盖当前文档的 actor、capability、outcome、business value、S1-S34 acceptance scenarios 和 out of scope。Loom review、PR merge、hosted check 或 agent 判断未用于替代确认。
- Previous confirmation source: 2026-07-15 当前 Codex 任务中用户明确回复“ok，确认通过，进入下一阶段”；该确认作为历史来源保留。
- Historical revision request: 2026-07-15 用户要求主线程先自我审核并将 Story 规范为 GWT；随后补充账号身份创建、技能驱动任务输入、Library 使用与分类筛选、细分结果消费、人工接管校验续跑及统一执行策略；本轮继续补齐指纹浏览器启动台的身份组织、对象关系、导入、代理网络、指纹配置、登录同步、启动恢复、异常处理、数据生命周期和任务往返。以上 revision 已由 head `993df044bbab900b0dc275e19588430602b2bf6f` 承载并获得用户确认。
- Follow-up confirmation: 2026-07-16 用户确认任务线程以“站点技能 + 账号身份”为稳定键，每次业务输入形成线程内任务回合，并要求 Work 同时支持“按技能”和“按身份”两种前端分组；随后确认分组与排序应收进 Codex-like 列表标题溢出菜单，且“按站点技能”不再重复增加站点外层。用户进一步确认中栏按时间顺序保留全部回合，执行动作默认收起，导航轨只负责定位，右侧文件预览只响应用户在中栏的明确打开动作。用户随后要求账号身份创建区分“需要账号登录”和“无需账号登录”，登录分支以目标 URL 拉起登录，免登录分支使用本地名称，并尽可能复用 CloakBrowser Manager 的 Profile 配置、一键随机与折叠高级设置；以上要求由 Scenario S12、S29、S31 和 S32 承载。
- Execution policy confirmation: 2026-07-17 用户确认执行策略由全局动作分类默认、“我的技能默认”、当前线程修订和当前动作单次决定组成；覆盖顺序为当前动作、当前线程、我的技能默认、全局。技能推荐配置只用于安装初始化，线程修订可显式保存为该技能的默认设置。系统不强制危险动作采用更严格方式，但必须核对技能声明与实际业务动作一致，并以常驻图标提示危险自动策略。以上确认由 Scenario S9 和 S14 承载。
- Revision request: none；2026-07-18 用户要求 Story 与高保真原型重新对齐：全局任务入口采用空状态创建页和“账号身份 + 站点技能”推荐组合；线程 Composer 固定上下文并只提交结构化业务输入；回合移除重复诊断和额外总结标题，增加时间与非 App 来源脚标；批量结果、右栏生命周期、左栏任务列表、身份集合与两种身份副本按最新原型收束；执行方式改用“读取和下载、填写但不提交、发布或提交、危险行为”及“我的技能默认”等用户语言，单次确认不再修改后续配置。本次修订已写入 S3、S4、S5、S6、S9、S11、S12、S14、S16、S17、S21、S29、S31、S32，并由 2026-07-19 本次确认关闭。
- Output view revision: 2026-07-18 用户要求站点技能可以声明适合自身业务结果的可选输出组件，类似 ChatGPT Apps 的丰富视图；结构化输出仍是结果事实，App 标准组件与通用视图负责兼容和兜底，自定义视图隔离运行且不能绕过执行方式。以上要求由 Scenario S34 承载，并由 2026-07-19 本次确认关闭。
- Audit revision: 2026-07-18 用户要求补齐单线程并发、草稿与附件生命周期、App/网络重连防重复、技能升级动作迁移、文件动作边界、批处理、丰富视图信任边界及横切可访问性与窄窗口验收，并去重账号身份创建场景。以上要求由 Scenario S7、S12-S14、S16-S19、S32-S34 承载，并由 2026-07-19 本次确认关闭。
- Bypass rationale, if `not_applicable`: not applicable
- Confirmation scope: actor, capability, outcome, business value, acceptance scenarios, out of scope.
- 当前确认允许 S1-S34 作为后续生产 UI Work Item 的业务语义输入；该确认不批准具体技术方案、实现质量或真实运行证据。

## Delivery Consumption Boundary

- Schema marker: `loom-story-delivery-mapping/v1`
- Intended Work Item or FR: App #298, anchored by App #308.
- Scenario locator export: `S1` through `S34`, using each scenario's `Scenario locator`.
- Business Confirmation locator export: `docs/stories/APP-308.md#story-business-confirmation`.
- Spec entry expectation: 后续 spec 通过导出的 scenario locator 消费已确认业务语义，不复制 Story 全文。
- Plan entry expectation: 将 S1-S34 映射到生产验证、检查或明确的 not-applicable evidence；高保真原型是已批准设计基线，不是生产实现证据。
- Story confirmation requirement: Story Readiness and Story Business Confirmation must both be `confirmed` or explicitly `not_applicable` before any scenario locator is exported.
- Story fields must not copy delivery handoff, recovery state, review findings, PR summary, merge-ready, closeout state, or formal spec / plan content.
